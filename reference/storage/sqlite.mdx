---
title: SQLite
---

SQLite Storage is a class that implements the Storage interface using SQLite as the backend storage system. It provides lightweight, file-based storage for sessions across agents, teams, and workflows, with dynamic table schema generation based on the chosen `mode` and support for JSON data types and schema versioning.

<Snippet file="storage-sqlite-reference.mdx" />