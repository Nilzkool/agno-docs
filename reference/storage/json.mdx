---
title: JSON
---

JSON Storage is a class that implements the Storage interface using JSON files as the backend storage system. It provides a simple, file-based storage solution for sessions across agents, teams, and workflows, with each session stored in a separate JSON file. The behavior is determined by the `mode` parameter passed during initialization.

<Snippet file="storage-json-reference.mdx" />