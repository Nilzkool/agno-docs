---
title: MongoDB
---

MongoDB Storage is a class that implements the Storage interface using MongoDB as the backend storage system. It provides scalable, document-based storage for sessions across agents, teams, and workflows, with support for mode-specific indexing and efficient querying. The behavior is determined by the `mode` parameter passed during initialization.

<Snippet file="storage-mongodb-reference.mdx" />