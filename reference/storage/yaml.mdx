---
title: YAML
---

YAML Storage is a class that implements the Storage interface using YAML files as the backend storage system. It provides a human-readable, file-based storage solution for sessions across agents, teams, and workflows, with each session stored in a separate YAML file. The behavior is determined by the `mode` parameter passed during initialization.

<Snippet file="storage-yaml-reference.mdx" />