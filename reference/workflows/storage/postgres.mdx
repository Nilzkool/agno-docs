---
title: Postgres Storage (for Workflows)
sidebarTitle: Postgres
---

This section details how to configure and use the generic `PostgresStorage` class specifically for managing workflow sessions. Initialize `PostgresStorage` with `mode="workflow"` to enable workflow-specific session handling and dynamic table schema generation.

<Snippet file="workflow-storage-postgres-params.mdx" />