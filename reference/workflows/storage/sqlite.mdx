---
title: SQLite Storage (for Workflows)
sidebarTitle: SQLite
---

This section details how to configure and use the generic `SqliteStorage` class specifically for managing workflow sessions. Initialize `SqliteStorage` with `mode="workflow"` to enable workflow-specific session handling and dynamic table schema generation.

<Snippet file="workflow-storage-sqlite-params.mdx" />