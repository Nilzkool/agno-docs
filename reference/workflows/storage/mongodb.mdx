---
title: MongoDB Storage (for Workflows)
sidebarTitle: MongoDB
---

This section details how to configure and use the generic `MongoDbStorage` class specifically for managing workflow sessions. Initialize `MongoDbStorage` with `mode="workflow"` to enable workflow-specific session handling and indexing.

<Snippet file="workflow-storage-mongodb-params.mdx" />