---
title: Workflow
sidebarTitle: Workflow
---

The `Workflow` class provides a structured way to define and execute sequences of operations, often involving multiple agents and tools, to achieve complex goals. When configuring a workflow with a storage backend, ensure the storage is initialized with `mode="workflow"` to correctly manage workflow sessions.

<Snippet file="workflow-reference.mdx" />