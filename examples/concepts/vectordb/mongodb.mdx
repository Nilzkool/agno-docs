---
title: MongoDB Integration
---

## Code

```python cookbook/agent_concepts/knowledge/vector_dbs/mongo_db/mongo_db.py
from agno.agent import Agent
from agno.knowledge.pdf_url import PDFUrlKnowledgeBase
from agno.vectordb.mongodb import MongoDb

# MongoDB Atlas connection string example
mdb_connection_string = "mongodb+srv://<username>:<password>@cluster0.mongodb.net/agno?retryWrites=true&w=majority"

knowledge_base = PDFUrlKnowledgeBase(
    urls=["https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf"],
    vector_db=MongoDb(
        collection_name="recipes",
        db_url=mdb_connection_string,
        search_index_name="recipes",  # specify your index name
    ),
)

# Comment out after first run
knowledge_base.load(recreate=False)

# Create and use the agent
agent = Agent(knowledge=knowledge_base, show_tool_calls=True)
agent.print_response("How to make Thai curry?", markdown=True)
```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install libraries">
    ```bash
    pip install -U pymongo pypdf openai agno
    ```
  </Step>

  <Step title="Run Agent">
    <CodeGroup>
    ```bash Mac
    python cookbook/agent_concepts/knowledge/vector_dbs/mongo_db/mongo_db.py
    ```

    ```bash Windows
    python cookbook/agent_concepts/knowledge/vector_dbs/mongo_db/mongo_db.py
    ```
    </CodeGroup>
  </Step>
</Steps>