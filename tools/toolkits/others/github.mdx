---title: Github---**GithubTools** enables an Agent to access Github repositories and perform tasks such as listing open pull requests, issues and more.## PrerequisitesThe following examples requires the `PyGithub` library and a Github access token which can be obtained from [here](https://github.com/settings/tokens).```shellpip install -U PyGithub``````shellexport GITHUB_ACCESS_TOKEN=***```## ExampleThe following agent will search Google for the latest news about "Mistral AI":```python cookbook/tools/github_tools.pyfrom agno.agent import Agentfrom agno.tools.github import GithubToolsagent = Agent(    instructions=[        "Use your tools to answer questions about the repo: agno-agi/agno",        "Do not create any issues or pull requests unless explicitly asked to do so",    ],    tools=[GithubTools()],    show_tool_calls=True,)agent.print_response("List open pull requests", markdown=True)```## Toolkit Params| Parameter                  | Type   | Default | Description                                                                                                 || -------------------------- | ------ | ------- | ----------------------------------------------------------------------------------------------------------- || `access_token`             | `str`  | `None`  | Github access token for authentication. If not provided, will use GITHUB_ACCESS_TOKEN environment variable. || `base_url`                 | `str`  | `None`  | Optional base URL for Github Enterprise installations.                                                      || `search_repositories`      | `bool` | `True`  | Enable searching Github repositories.                                                                       || `list_repositories`        | `bool` | `True`  | Enable listing repositories for a user/organization.                                                        || `get_repository`           | `bool` | `True`  | Enable getting repository details.                                                                          || `list_pull_requests`       | `bool` | `True`  | Enable listing pull requests for a repository.                                                              |## Toolkit Functions| Function                                 | Description                                                                                                                                                 || ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- || `search_repositories`                    | Searches GitHub repositories based on a query, with optional filters for language, repository, user, sort, order, page, and per_page.                                     || `list_repositories`                      | Lists repositories for the authenticated user or an organization, with optional filters for type and sort.                                                                    || `get_repository`                         | Gets details about a specific repository, including comprehensive statistics like languages, issues, PRs, and contributors.                                                   || `list_pull_requests`                     | Lists pull requests for a repository, with optional filters for state, head, base, sort, and direction.                                                                     || `get_pull_request`                       | Gets basic details about a specific pull request by number.                                                                                                                 || `get_pull_request_with_details`          | Gets comprehensive details about a specific pull request, including comments, commits, and files changed.                                                                 || `get_pull_request_changes`               | Gets the file changes in a pull request.                                                                                                                                    || `create_issue`                           | Creates a new issue in a repository.                                                                                                                                        || `get_issue`                              | Gets details about a specific issue.                                                                                                                                        || `list_issues`                            | Lists issues for a repository, with optional filters for state, labels, and assignee.                                                                                       || `get_issue_comments`                     | Gets comments on a specific issue.                                                                                                                                          || `create_pull_request`                    | Creates a new pull request from a head branch to a base branch.                                                                                                             || `create_review_request`                  | Creates a review request for a pull request, assigning reviewers or teams.                                                                                                  || `create_file`                            | Creates a new file in a repository with specified content.                                                                                                                  || `get_file_content`                       | Gets the content of a file from a repository.                                                                                                                               || `update_file`                            | Updates the content of an existing file in a repository.                                                                                                                    || `delete_file`                            | Deletes a file from a repository.                                                                                                                                           || `get_directory_content`                  | Gets the contents (files and subdirectories) of a directory in a repository.                                                                                                || `get_branch_content`                     | Gets the root content (files and directories) of a specified branch.                                                                                                        || `create_branch`                          | Creates a new branch from an existing branch or commit SHA.                                                                                                                 || `set_default_branch`                     | Sets the default branch for a repository.                                                                                                                                   || `search_code`                            | Searches for code across GitHub repositories using various filters for language, repository, user, path, and filename.                                                        || `search_issues_and_prs`                  | Searches for issues and pull requests across GitHub, with optional filters for state, type, repository, user, label, sort, order, page, and per_page.                           || `get_pull_request_count`                 | Gets the count of pull requests for a repository, with optional filters for state, author, base, and head.                                                                  || `get_repository_stars`                   | Gets the number of stars for a specific repository.                                                                                                                         || `get_pulls_by_query`                     | Gets pull requests for a repository based on a detailed query, including state, sort, direction, base, and head filters.                                                  || `get_pull_request_comments`              | Gets comments on a pull request.                                                                                                                                            || `create_pull_request_comment`            | Creates a comment on a specific line of code in a pull request.                                                                                                             || `edit_pull_request_comment`              | Edits an existing comment on a pull request.                                                                                                                                |## Developer Resources- View [Tools](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/github.py)- View [Cookbook](https://github.com/Nilzkool/agno/blob/main/cookbook/tools/github_tools.py)