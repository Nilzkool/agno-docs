<ResponseField name="model" type="agno.models.base.Model">
  Model to use for this Agent.
</ResponseField>
<ResponseField name="name" type="Optional[str]">
  Agent name
</ResponseField>
<ResponseField name="agent_id" type="Optional[str]">
  Agent UUID (autogenerated if not set)
</ResponseField>
<ResponseField name="introduction" type="Optional[str]">
  Agent introduction. This is added to the message history when a run is started.
</ResponseField>

<ResponseField name="user_id" type="Optional[str]">
  Default user ID to use for this agent.
</ResponseField>

<ResponseField name="session_id" type="Optional[str]">
  Default session ID to use for this agent (autogenerated if not set).
</ResponseField>
<ResponseField name="session_name" type="Optional[str]">
  Session name
</ResponseField>
<ResponseField name="session_state" type="Optional[Dict[str, Any]]">
  Session state (stored in the database to persist across runs)
</ResponseField>

<ResponseField name="context" type="Optional[Dict[str, Any]]">
  Context available for tools and prompt functions.
</ResponseField>
<ResponseField name="add_context" type="bool">
  If `True`, add the context to the user prompt.
</ResponseField>
<ResponseField name="resolve_context" type="bool">
  If `True`, resolve the context (i.e. call any functions in the context) before running the agent.
</ResponseField>

<ResponseField name="memory" type="Optional[Union[agno.memory.agent.AgentMemory, agno.memory.v2.memory.Memory]]">
  Agent Memory instance (e.g., `AgentMemory` or `Memory` v2).
</ResponseField>
<ResponseField name="enable_agentic_memory" type="bool">
  Enable the agent to manage memories of the user.
</ResponseField>
<ResponseField name="enable_user_memories" type="bool">
  If `True`, the agent creates/updates user memories at the end of runs.
</ResponseField>
<ResponseField name="add_memory_references" type="Optional[bool]">
  If `True`, the agent adds a reference to the user memories in the response.
</ResponseField>
<ResponseField name="enable_session_summaries" type="bool">
  If `True`, the agent creates/updates session summaries at the end of runs.
</ResponseField>
<ResponseField name="add_session_summary_references" type="Optional[bool]">
  If `True`, the agent adds a reference to the session summaries in the response.
</ResponseField>
<ResponseField name="add_history_to_messages" type="bool">
  If `True`, add messages from the chat history to the messages list sent to the Model.
</ResponseField>
<ResponseField name="num_history_responses" type="Optional[int]">
  Deprecated in favor of `num_history_runs`: Number of historical responses to add to the messages.
</ResponseField>
<ResponseField name="num_history_runs" type="int">
  Number of historical runs to include in the messages.
</ResponseField>

<ResponseField name="knowledge" type="Optional[agno.knowledge.agent.AgentKnowledge]">
  Agent Knowledge instance.
</ResponseField>
<ResponseField name="add_references" type="bool">
  Enable RAG by adding references from `AgentKnowledge` to the user prompt.
</ResponseField>
<ResponseField name="retriever" type="Optional[Callable[..., Optional[List[Dict]]]]">
  Retrieval function to get references.
  Signature: `def retriever(agent: Agent, query: str, num_documents: Optional[int], **kwargs) -> Optional[list[dict]]: ...`
</ResponseField>
<ResponseField name="references_format" type="Literal["json", "yaml"]
">
  Format for references (`"json"` or `"yaml"`).
</ResponseField>

<ResponseField name="storage" type="Optional[agno.storage.base.Storage]">
  Storage instance for persisting sessions and state.
</ResponseField>
<ResponseField name="extra_data" type="Optional[Dict[str, Any]]">
  Extra data stored with this agent.
</ResponseField>

<ResponseField name="tools" type="Optional[List[Union[agno.tools.toolkit.Toolkit, Callable, agno.tools.function.Function, Dict]]]">
  A list of tools provided to the Model.
</ResponseField>
<ResponseField name="show_tool_calls" type="bool">
  Show tool calls in Agent response.
</ResponseField>
<ResponseField name="tool_call_limit" type="Optional[int]">
  Maximum number of tool calls allowed.
</ResponseField>
<ResponseField name="tool_choice" type="Optional[Union[str, Dict[str, Any]]]">
  Controls which (if any) tool is called by the model.
  `"none"` means the model will not call a tool and instead generates a message.
  `"auto"` means the model can pick between generating a message or calling a tool.
  Specifying a particular function via `{"type: "function", "function": {"name": "my_function"}}`
  forces the model to call that tool.
</ResponseField>

<ResponseField name="reasoning" type="bool">
  Enable reasoning by working through the problem step by step.
</ResponseField>
<ResponseField name="reasoning_model" type="Optional[agno.models.base.Model]">
  Model to use for reasoning steps.
</ResponseField>
<ResponseField name="reasoning_agent" type="Optional[agno.agent.agent.Agent]">
  Custom Agent to use for reasoning steps.
</ResponseField>
<ResponseField name="reasoning_min_steps" type="int">
  Minimum number of reasoning steps.
</ResponseField>
<ResponseField name="reasoning_max_steps" type="int">
  Maximum number of reasoning steps.
</ResponseField>

<ResponseField name="read_chat_history" type="bool">
  Add a tool that allows the Model to read the chat history.
</ResponseField>
<ResponseField name="search_knowledge" type="bool">
  Add a tool that allows the Model to search the knowledge base (aka Agentic RAG).
  Added only if knowledge is provided.
</ResponseField>
<ResponseField name="update_knowledge" type="bool">
  Add a tool that allows the Model to update the knowledge base.
</ResponseField>
<ResponseField name="read_tool_call_history" type="bool">
  Add a tool that allows the Model to get the tool call history.
</ResponseField>

<ResponseField name="system_message" type="Optional[Union[str, Callable, agno.models.message.Message]]">
  Provide the system message as a string or function.
</ResponseField>
<ResponseField name="system_message_role" type="str">
  Role for the system message.
</ResponseField>
<ResponseField name="create_default_system_message" type="bool">
  If `True`, create a default system message using agent settings.
</ResponseField>
<ResponseField name="description" type="Optional[str]">
  A description of the Agent that is added to the start of the system message.
</ResponseField>
<ResponseField name="goal" type="Optional[str]">
  The goal of this task.
</ResponseField>
<ResponseField name="instructions" type="Optional[Union[str, List[str], Callable]]">
  List of instructions for the agent.
</ResponseField>
<ResponseField name="expected_output" type="Optional[str]">
  Provide the expected output from the Agent.
</ResponseField>
<ResponseField name="additional_context" type="Optional[str]">
  Additional context added to the end of the system message.
</ResponseField>
<ResponseField name="markdown" type="bool">
  If `True`, add instructions to format the output using markdown.
</ResponseField>
<ResponseField name="add_name_to_instructions" type="bool">
  If `True`, add the agent name to the instructions.
</ResponseField>
<ResponseField name="add_datetime_to_instructions" type="bool">
  If `True`, add the current datetime to the instructions to give the agent a sense of time.
  This allows for relative times like "tomorrow" to be used in the prompt.
</ResponseField>
<ResponseField name="timezone_identifier" type="Optional[str]">
  Allows for custom timezone for datetime instructions following the TZ Database format (e.g. "Etc/UTC").
</ResponseField>
<ResponseField name="add_state_in_messages" type="bool">
  If `True`, add the session state variables in the user and system messages.
</ResponseField>
<ResponseField name="add_messages" type="Optional[List[Union[Dict, agno.models.message.Message]]]">
  A list of extra messages added after the system message and before the user message.
</ResponseField>

<ResponseField name="user_message" type="Optional[Union[List, Dict, str, Callable, agno.models.message.Message]]">
  Provide the user message as a string, list, dict, or function.
  Note: this will ignore the message sent to the run function.
</ResponseField>
<ResponseField name="user_message_role" type="str">
  Role for the user message.
</ResponseField>
<ResponseField name="create_default_user_message" type="bool">
  If `True`, create a default user message using references and chat history.
</ResponseField>

<ResponseField name="retries" type="int">
  Number of retries to attempt.
</ResponseField>
<ResponseField name="delay_between_retries" type="int">
  Delay between retries (in seconds).
</ResponseField>
<ResponseField name="exponential_backoff" type="bool">
  If `True`, the delay between retries is doubled each time.
</ResponseField>

<ResponseField name="response_model" type="Optional[Type[pydantic.BaseModel]]">
  Provide a response model to get the response as a Pydantic model.
</ResponseField>
<ResponseField name="parse_response" type="bool">
  If `True`, the response from the Model is converted into the `response_model`.
  Otherwise, the response is returned as a JSON string.
</ResponseField>
<ResponseField name="structured_outputs" type="Optional[bool]">
  Use model enforced structured outputs if supported (e.g. OpenAIChat).
</ResponseField>
<ResponseField name="use_json_mode" type="bool">
  If `response_model` is set, sets the response mode of the model, i.e. if the model should explicitly respond with a JSON object instead of a Pydantic model.
</ResponseField>
<ResponseField name="save_response_to_file" type="Optional[str]">
  Save the response to a file.
</ResponseField>

<ResponseField name="stream" type="Optional[bool]">
  Stream the response from the Agent.
</ResponseField>
<ResponseField name="stream_intermediate_steps" type="bool">
  Stream the intermediate steps from the Agent.
</ResponseField>

<ResponseField name="team" type="Optional[List[agno.agent.agent.Agent]]">
  The team of agents that this agent can transfer tasks to.
</ResponseField>
<ResponseField name="team_data" type="Optional[Dict[str, Any]]">
  Extra data associated with the team this agent is part of.
</ResponseField>
<ResponseField name="role" type="Optional[str]">
  If this Agent is part of a team, this is the role of the agent in the team.
</ResponseField>
<ResponseField name="respond_directly" type="bool">
  If this Agent is part of a team, this member agent will respond directly to the user
  instead of passing the response to the leader agent.
</ResponseField>
<ResponseField name="add_transfer_instructions" type="bool">
  Add instructions for transferring tasks to team members.
</ResponseField>
<ResponseField name="team_response_separator" type="str">
  Separator between responses from the team.
</ResponseField>
<ResponseField name="team_session_id" type="Optional[str]">
  Optional team session ID, set by the team leader agent.
</ResponseField>
<ResponseField name="team_id" type="Optional[str]">
  Optional team ID. Indicates this agent is part of a team.
</ResponseField>

<ResponseField name="debug_mode" type="bool">
  Enable debug logs.
</ResponseField>
<ResponseField name="monitoring" type="bool">
  If `True`, logs Agent information to agno.com for monitoring.
</ResponseField>
<ResponseField name="telemetry" type="bool">
  If `True`, logs minimal telemetry for analytics.
</ResponseField>
