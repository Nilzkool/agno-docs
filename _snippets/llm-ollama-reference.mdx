<ResponseField name="id" type="str" default="llama3.1">
  ID of the Ollama model to use.
</ResponseField>
<ResponseField name="name" type="str" default="Ollama">
  Display name for the model.
</ResponseField>
<ResponseField name="format" type="Any" default="None">
  The format to return a response in (e.g., JSON schema for structured output).
</ResponseField>
<ResponseField name="options" type="Any" default="None">
  Additional model parameters such as temperature or top_p.
</ResponseField>
<ResponseField name="keep_alive" type="Union[float, str]" default="None">
  Controls how long the model will stay loaded following the request.
</ResponseField>
<ResponseField name="request_params" type="Dict[str, Any]" default="None">
  Extra request parameters to include in the API call.
</ResponseField>
<ResponseField name="host" type="str" default="None">
  Host address for the Ollama API endpoint.
</ResponseField>
<ResponseField name="timeout" type="Any" default="None">
  Timeout setting for API requests.
</ResponseField>
<ResponseField name="client_params" type="Dict[str, Any]" default="None">
  Additional `{key: value}` dict sent when initializing the Ollama client.
</ResponseField>