import { ResponseField } from 'components';

<!--
This snippet lists the constructor parameters for the Ollama model class in agno.
-->

<ResponseField name="id" type="str" default="llama3.1">
  ID of the Ollama model to use.
</ResponseField>

<ResponseField name="name" type="str" default="Ollama">
  Human-readable name of the Ollama model.
</ResponseField>

<ResponseField name="provider" type="str" default="Ollama">
  Identifier for the model provider.
</ResponseField>

<ResponseField name="supports_native_structured_outputs" type="bool" default="True">
  Whether Ollama supports native structured (Pydantic) outputs.
</ResponseField>

<ResponseField name="format" type="Any">
  Schema or format for structured outputs. Typically a JSON schema or Pydantic model.
</ResponseField>

<ResponseField name="options" type="Any">
  Additional Ollama API options, such as temperature or top_p.
</ResponseField>

<ResponseField name="keep_alive" type="Union[float, str]">
  Controls how long to keep the Ollama model loaded after a request.
</ResponseField>

<ResponseField name="request_params" type="Dict[str, Any]">
  Extra keyword arguments sent with each chat request.
</ResponseField>

<ResponseField name="host" type="str">
  URL or hostname for the Ollama server (e.g., "http://localhost:11434").
</ResponseField>

<ResponseField name="timeout" type="Any">
  Request timeout for Ollama client calls.
</ResponseField>

<ResponseField name="client_params" type="Dict[str, Any]">
  Additional parameters used when initializing the Ollama client.
</ResponseField>