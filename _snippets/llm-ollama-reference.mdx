<ResponseField name="id" type="str" default="llama3.1">
ID of the model to use.
</ResponseField>
<ResponseField name="name" type="str" default="Ollama">
Name for this Model (not sent to the API).
</ResponseField>
<ResponseField name="provider" type="str" default="Ollama">
Provider for this Model.
</ResponseField>
<ResponseField name="format" type="Any">
Response format to use (e.g., JSON schema or JSON object).
</ResponseField>
<ResponseField name="options" type="Any">
Additional model-specific parameters such as temperature or other options.
</ResponseField>
<ResponseField name="keep_alive" type="Union[float, str]">
Controls how long the model remains loaded in memory after a request.
</ResponseField>
<ResponseField name="request_params" type="Dict[str, Any]">
Extra request parameters sent with the chat payload.
</ResponseField>
<ResponseField name="host" type="str">
Hostname or URL of the Ollama server.
</ResponseField>
<ResponseField name="timeout" type="Any">
Network timeout for API requests.
</ResponseField>
<ResponseField name="client_params" type="Dict[str, Any]">
Additional `{key: value}` dict when initializing the Ollama client.
</ResponseField>
<ResponseField name="client" type="OllamaClient" default="None">
Provide a custom synchronous `ollama.Client` instance.
</ResponseField>
<ResponseField name="async_client" type="AsyncOllamaClient" default="None">
Provide a custom asynchronous `ollama.AsyncClient` instance.
</ResponseField>