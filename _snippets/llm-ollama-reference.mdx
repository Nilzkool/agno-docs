<ResponseField name="id" type="str" default="llama3.1">
  ID of the Ollama model to use.
</ResponseField>

<ResponseField name="name" type="str" default="Ollama">
  The display name of the model provider.
</ResponseField>

<ResponseField name="provider" type="str" default="Ollama">
  The name of the model provider.
</ResponseField>

<ResponseField name="supports_native_structured_outputs" type="bool" default="True">
  Whether the model supports native structured outputs via Pydantic schemas.
</ResponseField>

<ResponseField name="format" type="Any">
  Format schema for structured or JSON outputs (e.g., Pydantic model schema).
</ResponseField>

<ResponseField name="options" type="Any">
  Additional model parameters (e.g., temperature, top_p) passed to Ollama.
</ResponseField>

<ResponseField name="keep_alive" type="Union[float, str]">
  Controls how long the model stays loaded in memory after the request.
</ResponseField>

<ResponseField name="request_params" type="Dict[str, Any]">
  Extra request parameters to include in every API call.
</ResponseField>

<ResponseField name="host" type="str">
  Custom host URL for the Ollama API.
</ResponseField>

<ResponseField name="timeout" type="Any">
  Timeout setting for API requests.
</ResponseField>

<ResponseField name="client_params" type="Dict[str, Any]">
  Additional parameters for initializing the Ollama client.
</ResponseField>