<ResponseField name="name" type="str" default="Ollama">
  Name of the model provider.
</ResponseField>
<ResponseField name="id" type="str" default="llama3.1">
  ID of the Ollama model to use.
</ResponseField>
<ResponseField name="host" type="str">
  Host URL of the Ollama server (e.g., "http://localhost:11434").
</ResponseField>
<ResponseField name="timeout" type="Any">
  Timeout for API requests.
</ResponseField>
<ResponseField name="format" type="Any">
  Response format, such as JSON schemas for structured outputs.
</ResponseField>
<ResponseField name="options" type="Any">
  Additional model parameters (e.g., temperature).
</ResponseField>
<ResponseField name="keep_alive" type="Union[float, str]">
  Controls how long the model stays loaded after the request.
</ResponseField>
<ResponseField name="request_params" type="Dict[str, Any]">
  Extra key–value parameters to include on each chat request.
</ResponseField>
<ResponseField name="client_params" type="Dict[str, Any]" default="None">
  Extra key–value arguments when initializing the Ollama client.
</ResponseField>
<ResponseField name="client" type="ollama.Client" default="None">
  Provide an existing synchronous OllamaClient instance.
</ResponseField>
<ResponseField name="async_client" type="ollama.AsyncClient" default="None">
  Provide an existing asynchronous OllamaClient instance.
</ResponseField>