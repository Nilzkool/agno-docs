<ResponseField name="id" type="str" default="llama3.1">
  ID of the model to use.
</ResponseField>
<ResponseField name="host" type="str">
  Host for the Ollama API.
</ResponseField>
<ResponseField name="timeout" type="Any">
  Timeout for the Ollama client.
</ResponseField>
<ResponseField name="format" type="Any">
  The format to return a response in. Currently the only accepted value is json.
</ResponseField>
<ResponseField name="options" type="Any">
  Additional model parameters such as temperature.
</ResponseField>
<ResponseField name="keep_alive" type="Union[float, str]">
  Controls how long the model will stay loaded into memory following the request.
</ResponseField>
<ResponseField name="client_params" type="Dict[str, Any]" default="None">
  Additional `{key: value}` dict sent when initializing the `Ollama()` client.
</ResponseField>
<ResponseField name="client" type="ollama.Client" default="None">
  Provide your own `ollama.Client()` instance.
</ResponseField>
<ResponseField name="request_params" type="Dict[str, Any]" default="None">
  Additional parameters sent with each chat request.
</ResponseField>
<ResponseField name="supports_native_structured_outputs" type="bool" default="True">
  Whether to enable native structured outputs when `response_format` is a Pydantic BaseModel.
</ResponseField>