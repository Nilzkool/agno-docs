## Updating Production

To update your production application, use the `ag ws patch` command. This command will update existing resources for the active workspace.

<CodeGroup>

```bash all-resources
ag ws patch
```

```bash filtered-resources
ag ws patch --env prd --infra aws --group app
```

</CodeGroup>

This command handles updating the ECS task definition and services automatically.
