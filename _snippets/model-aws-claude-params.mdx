| Parameter           | Type                                    | Default                                      | Description                                                                                                                                                                 |
| ------------------- | --------------------------------------- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| id                  | `str`                                   | `"anthropic.claude-3-5-sonnet-20240620-v1:0"` | The specific model ID used for generating responses.                                                                                                                        |
| name                | `str`                                   | `"AwsBedrockAnthropicClaude"`              | The name identifier for the AWS Bedrock Claude agent.                                                                                                                       |
| provider            | `str`                                   | `"AwsBedrock"`                             | The provider of the model.                                                                                                                                                  |
| aws_access_key      | `Optional[str]`                         | `None`                                       | AWS access key for Bedrock authentication; falls back to `AWS_ACCESS_KEY` environment variable if not provided.                                                            |
| aws_secret_key      | `Optional[str]`                         | `None`                                       | AWS secret key for Bedrock authentication; falls back to `AWS_SECRET_KEY` environment variable if not provided.                                                            |
| aws_region          | `Optional[str]`                         | `None`                                       | AWS region for Bedrock service; falls back to `AWS_REGION` environment variable if not provided.                                                                           |
| session             | `Optional[Session]`                     | `None`                                       | A boto3 Session object to use for authentication instead of environment variables.                                                                                          |
| client              | `Optional[AnthropicBedrock]`            | `None`                                       | The synchronous client instance for making requests to AWS Bedrock Claude.                                                                                                  |
| async_client        | `Optional[AsyncAnthropicBedrock]`       | `None`                                       | The asynchronous client instance for making requests to AWS Bedrock Claude.                                                                                                 |
| max_tokens          | `int`                                   | `4096`                                       | The maximum number of tokens to generate in the response.                                                                                                                   |
| temperature         | `Optional[float]`                       | `None`                                       | The sampling temperature to use, between 0 and 2. Higher values (e.g., 0.8) make the output more random; lower values (e.g., 0.2) make it more deterministic.              |
| top_p               | `Optional[float]`                       | `None`                                       | Nucleus sampling parameter. The model considers only tokens with cumulative probability up to `top_p`.                                                                      |
| top_k               | `Optional[int]`                         | `None`                                       | Top-k sampling parameter. Limits the next‐token candidates to the `top_k` highest-probability tokens.                                                                       |
| stop_sequences      | `Optional[List[str]]`                   | `None`                                       | Sequences where the API will stop generating further tokens.                                                                                                                |
| request_params      | `Optional[Dict[str, Any]]`              | `None`                                       | Additional request parameters for the Bedrock API, supplied as a dictionary.                                                                                                |
| client_params       | `Optional[Dict[str, Any]]`              | `None`                                       | Additional client parameters when initializing the AWS Bedrock client, provided as a dictionary.                                                                            |
