| Parameter           | Type                                 | Default                                      | Description                                                                                             |
| ------------------- | ------------------------------------ | -------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `id`                | `str`                                | `"anthropic.claude-3-5-sonnet-20240620-v1:0"` | The specific model ID used for generating responses.                                                     |
| `name`              | `str`                                | `"AwsBedrockAnthropicClaude"`                | The name identifier for the AWS Bedrock Claude agent.                                                   |
| `provider`          | `str`                                | `"AwsBedrock"`                               | The provider of the model.                                                                              |
| `aws_access_key`    | `Optional[str]`                      | `None`                                       | AWS access key for authentication; falls back to `AWS_ACCESS_KEY` environment variable.                |
| `aws_secret_key`    | `Optional[str]`                      | `None`                                       | AWS secret key for authentication; falls back to `AWS_SECRET_KEY` environment variable.                |
| `aws_region`        | `Optional[str]`                      | `None`                                       | AWS region to use; falls back to `AWS_REGION` environment variable.                                      |
| `session`           | `Optional[boto3.session.Session]`    | `None`                                       | A boto3 Session object for authentication (credentials and region).                                     |
| `client`            | `Optional[AnthropicBedrock]`         | `None`                                       | The synchronous client for making requests to the Anthropic Bedrock service.                            |
| `async_client`      | `Optional[AsyncAnthropicBedrock]`    | `None`                                       | The asynchronous client for making requests to the Anthropic Bedrock service.                           |
| `max_tokens`        | `int`                                | `4096`                                       | The maximum number of tokens to generate in the response.                                               |
| `temperature`       | `Optional[float]`                    | `None`                                       | Sampling temperature between 0 and 2; higher values produce more random output.                         |
| `top_p`             | `Optional[float]`                    | `None`                                       | Nucleus sampling parameter; model considers tokens comprising top_p probability mass.                   |
| `top_k`             | `Optional[int]`                      | `None`                                       | Number of highest-probability tokens to keep for top-k filtering.                                       |
| `stop_sequences`    | `Optional[List[str]]`                | `None`                                       | List of sequences where the API will stop generating further tokens.                                    |
| `request_params`    | `Optional[Dict[str, Any]]`           | `None`                                       | Additional API request parameters as a dictionary.                                                      |
| `client_params`     | `Optional[Dict[str, Any]]`           | `None`                                       | Additional parameters for initializing the Bedrock client, as a dictionary.                             |