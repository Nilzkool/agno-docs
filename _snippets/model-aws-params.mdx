| Parameter                | Type                       | Default                                   | Description                                                                                                                                                          |
| ------------------------ | -------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                     | `str`                      | `"mistral.mistral-small-2402-v1:0"`     | The Bedrock model ID to use for chat completions.                                                                                                                   |
| `name`                   | `str`                      | `"AwsBedrock"`                          | The name identifier for the AWS Bedrock model.                                                                                                                      |
| `provider`               | `str`                      | `"AwsBedrock"`                          | The provider of the model.                                                                                                                                           |
| `aws_region`             | `Optional[str]`            | `None`                                    | AWS region to use for the Bedrock client. Can also be set via the `AWS_REGION` environment variable.                                                                 |
| `aws_access_key_id`      | `Optional[str]`            | `None`                                    | AWS access key ID for authentication. Can also be set via the `AWS_ACCESS_KEY_ID` environment variable.                                                              |
| `aws_secret_access_key`  | `Optional[str]`            | `None`                                    | AWS secret access key for authentication. Can also be set via the `AWS_SECRET_ACCESS_KEY` environment variable.                                                       |
| `session`                | `Optional[Session]`        | `None`                                    | A boto3 `Session` object to use for authentication instead of environment variables.                                                                                |
| `max_tokens`             | `Optional[int]`            | `None`                                    | The maximum number of tokens to generate in the response.                                                                                                            |
| `temperature`            | `Optional[float]`          | `None`                                    | The sampling temperature, between 0 and 2. Higher values make output more random.                                                                                    |
| `top_p`                  | `Optional[float]`          | `None`                                    | The nucleus sampling parameter; the model considers tokens comprising the top_p probability mass.                                                                    |
| `stop_sequences`         | `Optional[List[str]]`      | `None`                                    | A list of sequences where the API will stop generating further tokens.                                                                                              |
| `request_params`         | `Optional[Dict[str, Any]]` | `None`                                    | Additional inference parameters to pass directly to the AWS Bedrock API.                                                                                             |