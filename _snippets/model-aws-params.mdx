| Parameter               | Type                       | Default                              | Description                                                       |
| ----------------------- | -------------------------- | ------------------------------------ | ----------------------------------------------------------------- |
| `id`                    | `str`                      | `"mistral.mistral-small-2402-v1:0"` | The specific AWS Bedrock model identifier to use.                 |
| `aws_region`            | `Optional[str]`            | `None`                               | AWS region for the Bedrock client (from `AWS_REGION`).           |
| `aws_access_key_id`     | `Optional[str]`            | `None`                               | AWS access key ID (from `AWS_ACCESS_KEY_ID`).                    |
| `aws_secret_access_key` | `Optional[str]`            | `None`                               | AWS secret access key (from `AWS_SECRET_ACCESS_KEY`).            |
| `session`               | `Optional[Session]`        | `None`                               | Boto3 `Session` object for authentication.                       |
| `client`                | `Optional[AwsClient]`      | `None`                               | The underlying boto3 Bedrock client instance.                    |
| `max_tokens`            | `Optional[int]`            | `None`                               | The maximum number of tokens to generate in the response.        |
| `temperature`           | `Optional[float]`          | `None`                               | Sampling temperature.                                            |
| `top_p`                 | `Optional[float]`          | `None`                               | Nucleus sampling parameter.                                      |
| `stop_sequences`        | `Optional[List[str]]`      | `None`                               | Sequences where the model will stop generating further tokens.   |
| `request_params`        | `Optional[Dict[str, Any]]` | `None`                               | Additional inference configuration overrides.                    |
