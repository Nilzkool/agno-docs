| Parameter               | Type                        | Default                             | Description                                                                                           |
| ----------------------- | --------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `id`                    | `str`                       | `"mistral.mistral-small-2402-v1:0"` | The specific model ID used for generating responses.                                                  |
| `name`                  | `str`                       | `"AwsBedrock"`                    | The name identifier for the AWS Bedrock model.                                                        |
| `provider`              | `str`                       | `"AwsBedrock"`                    | The provider of the model.                                                                            |
| `aws_region`            | `Optional[str]`             | `None`                              | The AWS region to use (overrides `AWS_REGION` environment variable).                                  |
| `aws_access_key_id`     | `Optional[str]`             | `None`                              | The AWS access key ID to use (overrides `AWS_ACCESS_KEY_ID` environment variable).                    |
| `aws_secret_access_key` | `Optional[str]`             | `None`                              | The AWS secret access key to use (overrides `AWS_SECRET_ACCESS_KEY` environment variable).            |
| `session`               | `Optional[Session]`         | `None`                              | A boto3 `Session` object for authentication.                                                          |
| `max_tokens`            | `Optional[int]`             | `None`                              | The maximum number of tokens to generate in the response.                                             |
| `temperature`           | `Optional[float]`           | `None`                              | The sampling temperature to use, between 0 and 2.                                                      |
| `top_p`                 | `Optional[float]`           | `None`                              | The nucleus sampling parameter for token selection.                                                  |
| `stop_sequences`        | `Optional[List[str]]`       | `None`                              | A list of sequences where the API will stop generating further tokens.                                |
| `request_params`        | `Optional[Dict[str, Any]]`  | `None`                              | Additional parameters for the request, provided as a dictionary (merged into inference configuration). |