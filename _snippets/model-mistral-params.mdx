| Parameter         | Type                                                              | Default                  | Description                                                                                                                   |
|-------------------|-------------------------------------------------------------------|--------------------------|-------------------------------------------------------------------------------------------------------------------------------|
| `id`              | `str`                                                             | `"mistral-large-latest"` | The ID of the model.                                                                                                         |
| `name`            | `str`                                                             | `"MistralChat"`          | The name of the model.                                                                                                       |
| `provider`        | `str`                                                             | `"Mistral"`              | The provider of the model.                                                                                                   |
| `temperature`     | `Optional[float]`                                                 | `None`                   | Controls randomness in output generation.                                                                                   |
| `max_tokens`      | `Optional[int]`                                                   | `None`                   | Maximum number of tokens to generate.                                                                                        |
| `top_p`           | `Optional[float]`                                                 | `None`                   | Controls diversity of output generation.                                                                                     |
| `random_seed`     | `Optional[int]`                                                   | `None`                   | Seed for random number generation.                                                                                           |
| `safe_mode`       | `bool`                                                            | `False`                  | Enables content filtering.                                                                                                   |
| `safe_prompt`     | `bool`                                                            | `False`                  | Applies content filtering to prompts.                                                                                        |
| `response_format` | `Optional[Union[Dict[str, Any], ChatCompletionResponse]]`         | `None`                   | Specifies the desired response format (structured outputs or native format).                                                |
| `request_params`  | `Optional[Dict[str, Any]]`                                        | `None`                   | Additional request parameters to pass through to the Mistral API.                                                            |
| `api_key`         | `Optional[str]`                                                   | `None`                   | Your Mistral API key; also read from the `MISTRAL_API_KEY` environment variable if not set.                                  |
| `endpoint`        | `Optional[str]`                                                   | `None`                   | Custom API endpoint URL; overrides the default Mistral endpoint when provided.                                               |
| `max_retries`     | `Optional[int]`                                                   | `None`                   | Maximum number of retries for API calls on failure.                                                                           |
| `timeout`         | `Optional[int]`                                                   | `None`                   | Timeout for API calls in seconds.                                                                                             |
| `client_params`   | `Optional[Dict[str, Any]]`                                        | `None`                   | Additional parameters to initialize the underlying `MistralClient`.                                                          |
| `mistral_client`  | `Optional[MistralClient]`                                         | `None`                   | Provide a custom `MistralClient` instance; if set, this client will be used instead of creating a new one.                   |