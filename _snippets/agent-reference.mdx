## Parameters

| Parameter                         | Type                              | Default   | Description                                                                                                                      |
|-----------------------------------|-----------------------------------|-----------|----------------------------------------------------------------------------------------------------------------------------------|
| `model`                           | `Optional[Model]`                 | `None`    | Model to use for this Agent                                                                                                      |
| `name`                            | `Optional[str]`                   | `None`    | Agent name                                                                                                                       |
| `agent_id`                        | `Optional[str]`                   | `None`    | Agent UUID (autogenerated if not set)                                                                                           |
| `introduction`                    | `Optional[str]`                   | `None`    | Agent introduction. This is added to the chat history when a run is started                                                      |
| `user_id`                         | `Optional[str]`                   | `None`    | Default user_id to use for this agent                                                                                            |
| `session_id`                      | `Optional[str]`                   | `None`    | Default session_id to use for this agent (autogenerated if not set)                                                               |
| `session_name`                    | `Optional[str]`                   | `None`    | Session name                                                                                                                     |
| `session_state`                   | `Optional[Dict[str, Any]]`        | `None`    | Session state (stored in the database to persist across runs)                                                                     |
| `context`                         | `Optional[Dict[str, Any]]`        | `None`    | Context available for tools and prompt functions                                                                                 |
| `add_context`                     | `bool`                            | `False`   | If True, add the context to the user prompt                                                                                      |
| `resolve_context`                 | `bool`                            | `True`    | If True, resolve the context (i.e. call any functions in the context) before running the agent                                   |
| `memory`                          | `Optional[Union[AgentMemory, Memory]]` | `None`    | Agent memory instance (v1 or v2)                                                                                                  |
| `enable_agentic_memory`          | `bool`                            | `False`   | Enable the agent to manage memories of the user                                                                                  |
| `enable_user_memories`            | `bool`                            | `False`   | If True, the agent creates/updates user memories at the end of runs                                                              |
| `add_memory_references`           | `Optional[bool]`                  | `None`    | If True, the agent adds a reference to the user memories in the response                                                        |
| `enable_session_summaries`        | `bool`                            | `False`   | If True, the agent creates/updates session summaries at the end of runs                                                          |
| `add_session_summary_references`  | `Optional[bool]`                  | `None`    | If True, the agent adds a reference to the session summaries in the response                                                    |
| `add_history_to_messages`         | `bool`                            | `False`   | If True, add messages from chat history to the messages list sent to the model                                                   |
| `num_history_responses`           | `Optional[int]`                   | `None`    | Deprecated in favor of `num_history_runs`; number of historical responses to add                                                 |
| `num_history_runs`                | `int`                             | `3`       | Number of historical runs to include in the messages                                                                             |
| `knowledge`                       | `Optional[AgentKnowledge]`        | `None`    | Agent knowledge base                                                                                                             |
| `add_references`                  | `bool`                            | `False`   | If True, add retrieval references from the knowledge base to the user prompt                                                     |
| `retriever`                       | `Optional[Callable[..., Optional[List[Dict]]]]` | `None`    | Custom retrieval function signature for references                                                                              |
| `references_format`               | `Literal['json','yaml']`          | `'json'`  | Format for rendering references                                                                                                  |
| `storage`                         | `Optional[Storage]`               | `None`    | Storage backend for persisting sessions                                                                                          |
| `extra_data`                      | `Optional[Dict[str, Any]]`        | `None`    | Extra data stored with this agent                                                                                                |
| `tools`                           | `Optional[List[Union[Toolkit, Callable, Function, Dict]]]` | `None`    | Tools or toolkits available to the agent                                                                                         |
| `show_tool_calls`                 | `bool`                            | `True`    | Whether to include tool call information in the agent response                                                                   |
| `tool_call_limit`                 | `Optional[int]`                   | `None`    | Maximum number of tool calls allowed                                                                                             |
| `tool_choice`                     | `Optional[Union[str, Dict[str,Any]]]` | `None`    | Controls which tool the model may call (`none`, `auto`, or specific function)                                                   |
| `reasoning`                       | `bool`                            | `False`   | Enable step-by-step reasoning                                                                                                     |
| `reasoning_model`                 | `Optional[Model]`                 | `None`    | Alternate model for reasoning                                                                                                     |
| `reasoning_agent`                 | `Optional[Agent]`                 | `None`    | Agent wrapper for structured reasoning loops                                                                                      |
| `reasoning_min_steps`             | `int`                             | `1`       | Minimum reasoning steps                                                                                                           |
| `reasoning_max_steps`             | `int`                             | `10`      | Maximum reasoning steps                                                                                                           |
| `read_chat_history`               | `bool`                            | `False`   | Add a function to fetch chat history                                                                                             |
| `search_knowledge`                | `bool`                            | `True`    | Add a function to retrieve from knowledge base                                                                                   |
| `update_knowledge`                | `bool`                            | `False`   | Add a function to update the knowledge base                                                                                      |
| `read_tool_call_history`          | `bool`                            | `False`   | Add a function to fetch past tool calls                                                                                          |
| `system_message`                  | `Optional[Union[str, Callable, Message]]` | `None`    | Custom system message content or generator                                                                                       |
| `system_message_role`             | `str`                             | `'system'`| Role name for system message                                                                                                     |
| `create_default_system_message`   | `bool`                            | `True`    | Whether to build a default system prompt                                                                                          |
| ...                               | ...                               | ...       | ... (full list updated to match all fields in `Agent` constructor)                                                                |

## Functions

| Function             | Description                                                  |
|----------------------|--------------------------------------------------------------|
| `run`                | Synchronous run of the agent                                  |
| `arun`               | Asynchronous run of the agent                                 |
| `print_response`     | Run the agent and render output in console with rich panels    |
| `aprint_response`    | Async print response                                          |
| `get_session_summary`| Retrieve session summary                                      |
| `get_user_memories`  | Retrieve user memories                                        |