| Name                            | Type                              | Default          | Description                                                                                                             |
| ------------------------------- | --------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `id`                            | `str`                             | `"gpt-4o"`     | The model identifier to use (e.g., `gpt-4o`).                                                                            |
| `name`                          | `str`                             | `"OpenAIChat"` | The name of this chat model instance.                                                                                   |
| `provider`                      | `str`                             | `"OpenAI"`     | The provider of the model (always `OpenAI`).                                                                            |
| `store`                         | `Optional[bool]`                  | `None`           | Whether to store the API responses for evals or fine-tuning.                                                            |
| `reasoning_effort`              | `Optional[str]`                   | `None`           | Controls reasoning effort passed to OpenAI when supported.                                                              |
| `metadata`                      | `Optional[Dict[str, Any]]`        | `None`           | Additional metadata to include with the request.                                                                        |
| `frequency_penalty`             | `Optional[float]`                 | `None`           | Penalizes new tokens based on their existing frequency in the text.                                                     |
| `presence_penalty`              | `Optional[float]`                 | `None`           | Penalizes new tokens based on whether they appear in the text so far.                                                   |
| `logit_bias`                    | `Optional[Any]`                   | `None`           | Modifies the likelihood of specified tokens appearing in the completion.                                                |
| `logprobs`                      | `Optional[bool]`                  | `None`           | Include the log probabilities of the most likely tokens.                                                               |
| `top_logprobs`                  | `Optional[int]`                   | `None`           | How many log probability options to return per token.                                                                  |
| `max_tokens`                    | `Optional[int]`                   | `None`           | The maximum number of tokens to generate in the completion.                                                             |
| `max_completion_tokens`         | `Optional[int]`                   | `None`           | Alias for `max_tokens` when used as a request parameter.                                                               |
| `modalities`                    | `Optional[List[str]]`             | `None`           | Which modalities to request (`text`, `audio`).                                                                          |
| `audio`                         | `Optional[Dict[str, Any]]`        | `None`           | Audio generation parameters: e.g. `{voice:..., format:...}`.                                                           |
| `response_format`               | `Optional[Any]`                   | `None`           | A Pydantic model or dict schema to enforce structured output.                                                          |
| `seed`                          | `Optional[int]`                   | `None`           | A seed for deterministic sampling.                                                                                      |
| `stop`                          | `Optional[Union[str, List[str]]]` | `None`           | Sequences where the API will stop generating further tokens.                                                            |
| `temperature`                   | `Optional[float]`                 | `None`           | Controls randomness in the model’s output.                                                                              |
| `top_p`                         | `Optional[float]`                 | `None`           | Controls diversity via nucleus sampling.                                                                                |
| `user`                          | `Optional[str]`                   | `None`           | A unique end-user identifier.                                                                                           |
| `extra_headers`                 | `Optional[Any]`                   | `None`           | Additional HTTP headers to send with the request.                                                                       |
| `extra_query`                   | `Optional[Any]`                   | `None`           | Additional query string parameters for the request.                                                                     |
| `request_params`                | `Optional[Dict[str, Any]]`        | `None`           | Arbitrary request parameters to pass through.                                                                           |
| `api_key`                       | `Optional[str]`                   | `None`           | The OpenAI API key for authentication (ENV `OPENAI_API_KEY`).                                                            |
| `organization`                  | `Optional[str]`                   | `None`           | The OpenAI organization to use for billing and quotas.                                                                  |
| `base_url`                      | `Optional[Union[str, httpx.URL]]` | `None`           | Override the default OpenAI API base URL.                                                                               |
| `timeout`                       | `Optional[float]`                 | `None`           | Timeout in seconds for API requests.                                                                                    |
| `max_retries`                   | `Optional[int]`                   | `None`           | Number of times to retry on transient failures.                                                                         |
| `default_headers`               | `Optional[Any]`                   | `None`           | Default HTTP headers included with every request.                                                                       |
| `default_query`                 | `Optional[Any]`                   | `None`           | Default query parameters included with every request.                                                                   |
| `http_client`                   | `Optional[httpx.Client]`          | `None`           | A pre-configured HTTPX client instance to reuse.                                                                        |
| `client_params`                 | `Optional[Dict[str, Any]]`        | `None`           | Extra parameters for constructing the OpenAI client.                                                                    |
| `client`                        | `Optional[OpenAIClient]`          | `None`           | Cached synchronous OpenAI client instance.                                                                              |
| `async_client`                  | `Optional[AsyncOpenAIClient]`     | `None`           | Cached asynchronous OpenAI client instance.                                                                             |
| `structured_outputs`            | `bool`                            | `False`          | Whether to use native structured outputs when supported.                                                               |
| `supports_native_structured_outputs` | `bool`                    | `True`           | True if the model can emit structured outputs without JSON schema mode.                                                |