export const meta = {
  title: 'OpenAIChat Model Parameters',
};

| Name                            | Type                                | Default     | Description                                                                                                        |
| ------------------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| `id`                            | `string`                            | `"gpt-4o"`| The identifier of the OpenAI model to use (e.g., `gpt-4o`).                                                        |
| `name`                          | `string`                            | `"OpenAIChat"` | The name of this chat model instance.                                                                       |
| `provider`                      | `string`                            | `"OpenAI"`| The provider name for this model.                                                                                 |
| `supports_native_structured_outputs` | `boolean`                   | `true`      | Whether the model supports native Pydantic structured outputs.                                                    |
| `store`                         | `boolean?`                          | `undefined` | If true, store the output for use in model evaluation or distillation.                                            |
| `reasoning_effort`              | `string?`                           | `undefined` | Internal OpenAI header to specify reasoning effort level.                                                         |
| `metadata`                      | `Record<string, any>?`              | `undefined` | Additional metadata to include with the request.                                                                  |
| `frequency_penalty`             | `number?`                           | `undefined` | Penalizes new tokens based on their frequency in the text so far.                                                 |
| `logit_bias`                    | `any?`                              | `undefined` | Adjusts the likelihood of specified tokens appearing in the completion.                                           |
| `logprobs`                      | `boolean?`                          | `undefined` | Include log probabilities for the most likely tokens.                                                             |
| `top_logprobs`                  | `number?`                           | `undefined` | Number of top log probability tokens to return.                                                                   |
| `max_tokens`                    | `number?`                           | `undefined` | Maximum number of tokens to generate in the completion.                                                           |
| `max_completion_tokens`         | `number?`                           | `undefined` | Alias for maximum completion tokens (same as `max_tokens`).                                                      |
| `modalities`                    | `string[]?`                         | `undefined` | List of supported input/output modalities (e.g., `["text","audio"]`).                                         |
| `audio`                         | `Record<string, any>?`              | `undefined` | Audio-specific parameters such as `voice` or `format`.                                                            |
| `presence_penalty`              | `number?`                           | `undefined` | Penalizes new tokens based on whether they appear in the text so far.                                             |
| `response_format`               | `any?`                              | `undefined` | Schema or Pydantic model specifying output structure.                                                             |
| `seed`                          | `number?`                           | `undefined` | Seed for deterministic sampling.                                                                                  |
| `stop`                          | `string \| string[]?`              | `undefined` | Up to 4 sequences where the API will stop generating further tokens.                                              |
| `temperature`                   | `number?`                           | `undefined` | Sampling temperature to control randomness.                                                                       |
| `user`                          | `string?`                           | `undefined` | A unique identifier for the end user.                                                                             |
| `top_p`                         | `number?`                           | `undefined` | Nucleus sampling parameter controlling diversity.                                                                 |
| `extra_headers`                 | `Record<string, any>?`              | `undefined` | Additional HTTP headers to include with the request.                                                              |
| `extra_query`                   | `Record<string, any>?`              | `undefined` | Additional query parameters for the request.                                                                      |
| `request_params`                | `Record<string, any>?`              | `undefined` | Extra parameters to send directly to the API call.                                                                |
| `api_key`                       | `string?`                           | `undefined` | OpenAI API key for authentication (falls back to `OPENAI_API_KEY` env var).                                        |
| `organization`                  | `string?`                           | `undefined` | OpenAI organization ID for API requests (optional).                                                               |
| `base_url`                      | `string \| URL?`                   | `undefined` | Custom base URL for the OpenAI API endpoint.                                                                      |
| `timeout`                       | `number?`                           | `undefined` | Request timeout in seconds.                                                                                       |
| `max_retries`                   | `number?`                           | `undefined` | Maximum number of retries for failed HTTP requests.                                                               |
| `default_headers`               | `Record<string, any>?`              | `undefined` | Default HTTP headers for the client.                                                                              |
| `default_query`                 | `Record<string, any>?`              | `undefined` | Default HTTP query parameters for the client.                                                                     |
| `http_client`                   | `httpx.Client?`                     | `undefined` | Pre-configured `httpx` client instance.                                                                           |
| `client_params`                 | `Record<string, any>?`              | `undefined` | Additional parameters for initializing the OpenAI client.                                                         |
| `client`                        | `OpenAIClient?`                     | `undefined` | Internal synchronous OpenAI client instance.                                                                      |
| `async_client`                  | `AsyncOpenAIClient?`                | `undefined` | Internal asynchronous OpenAI client instance.                                                                     |
| `structured_outputs`            | `boolean`                           | `false`     | If true, enforce Pydantic model output when `response_format` is a Pydantic class.                                |