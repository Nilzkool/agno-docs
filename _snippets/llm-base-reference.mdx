| Parameter               | Type                     | Default | Description                                                                                              |
|-------------------------|--------------------------|---------|----------------------------------------------------------------------------------------------------------|
| `id`                    | `str`                    | -       | ID of the model to use.                                                                                  |
| `name`                  | `str`                    | -       | Name for this Model. Not sent to the provider API.                                                       |
| `provider`              | `str`                    | -       | Provider identifier for this Model.                                                                       |
| `api_key`               | `Optional[str]`          | -       | API key to authenticate with the provider (can also be set via `LITELLM_API_KEY`).                        |
| `api_base`              | `Optional[str]`          | -       | Base URL for the provider API.                                                                            |
| `max_tokens`            | `Optional[int]`          | -       | Maximum number of tokens to generate.                                                                     |
| `temperature`           | `float`                  | `0.7`   | Sampling temperature.                                                                                     |
| `top_p`                 | `float`                  | `1.0`   | Nucleus sampling probability.                                                                             |
| `request_params`        | `Optional[Dict[str, Any]]`| -      | Additional parameters to include in each API request.                                                     |
| `response_format`       | `Any`                    | -       | Format of the response, e.g. JSON schema or model class for structured outputs.                          |
| `structured_outputs`    | `bool`                   | `False` | Whether to enforce structured output mode when supported.                                                 |
| `tools`                 | `List[Dict]`             | -       | List of tools (functions) provided to the model via the Agent.                                           |
| `tool_choice`           | `Union[str, Dict[str,Any]]`| -     | Strategy for tool calls: `