| Parameter        | Type                             | Default       | Description                                                    |
| ---------------- | -------------------------------- | ------------- | -------------------------------------------------------------- |
| `id`             | `str`                            | `"gpt-4o"`   | The model ID or deployment name for Azure AI Foundry. Required. |
| `name`           | `str`                            | `"AzureAIFoundry"` | The name identifier for this Azure AI Foundry model.            |
| `provider`       | `str`                            | `"Azure"`    | The provider name.                                             |
| `temperature`    | `Optional[float]`                | `None`        | Controls randomness in model output. Higher values increase it. |
| `max_tokens`     | `Optional[int]`                  | `None`        | Maximum number of tokens to generate.                          |
| `frequency_penalty` | `Optional[float]`             | `None`        | Penalizes new tokens based on their existing frequency.        |
| `presence_penalty`  | `Optional[float]`             | `None`        | Penalizes new tokens based on whether they appear at all.      |
| `top_p`          | `Optional[float]`                | `None`        | Limits the cumulative probability of tokens considered.        |
| `stop`           | `Optional[Union[str, List[str]]]` | `None`        | Sequences at which to stop generation.                         |
| `seed`           | `Optional[int]`                  | `None`        | Random seed for deterministic outputs.                         |
| `model_extras`   | `Optional[Dict[str, Any]]`       | `None`        | Additional model-specific parameters.                         |
| `request_params` | `Optional[Dict[str, Any]]`       | `None`        | Extra parameters to include in the API request.                |
| `api_key`        | `Optional[str]`                  | `None`        | Your Azure AI Foundry API key.                                 |
| `api_version`    | `Optional[str]`                  | `None`        | API version (env default `2024-05-01-preview` if unset).       |
| `azure_endpoint` | `Optional[str]`                  | `None`        | Endpoint URL for your Azure AI Foundry resource.               |
| `timeout`        | `Optional[float]`                | `None`        | Request timeout in seconds.                                    |
| `max_retries`    | `Optional[int]`                  | `None`        | Maximum retry attempts on failure.                             |
| `http_client`    | `Optional[httpx.Client]`         | `None`        | Custom HTTP client instance for requests.                      |
| `client_params`  | `Optional[Dict[str, Any]]`       | `None`        | Additional client configuration parameters.                   |